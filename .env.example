# Copy this file to ".env" and replace placeholder values.
# Never commit ".env" because it may contain secrets.

# Server
PORT=19880

# Database (MySQL)
DB_NAME=giftverse
DB_ENDPOINT=localhost
USER=root
PASSWORD=change_me
